language: c
compiler:
   - gcc

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install libsdl2-dev

script:
  - make

notifications:
  email: false

  irc:
    channels:
      - "irc.blitzed.org#3dmoo"
    on_success: always
    on_failure: always
